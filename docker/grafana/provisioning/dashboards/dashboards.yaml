apiVersion: 1

providers:
  - name: batch-dtu
    orgId: 1
    folder: "Batch DTU"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
