# Coleta bruta do inventário legado: contratos, hostnames, nomes de cliente e caminhos internos.
# NUNCA comitar. Ver seed/collect/README.md.
seed/raw/*
!seed/raw/.gitkeep

# Pacote de coleta solto, em QUALQUER diretório (o coletor entrega um .tar.gz;
# o lugar dele é seed/raw/, mas cópias aparecem onde a coleta foi baixada).
batch-seed-*
batch-inventory-*
*.tar.gz

# Python
__pycache__/
*.pyc

# Ambiente virtual
.venv/

# Ambiente local
.env
docker/legacy/keys/

# Build artifacts
*.egg-info/
build/
dist/
