# ADR-004 — Estratégia para `execute_command` Remoto

- **Status**: aberto (dependente do inventário de scripts — Risco 6)

## Opções

**(a)** Manter SSH gerenciado pelo executor contra hosts-alvo registrados no catálogo.

**(b)** Implantar agentes/endpoints leves nos hosts-alvo.

## Dependência

Resultado do inventário de `custom_scripts/` e mapeamento de dependências (auditoria de árvore de diretórios em andamento).
